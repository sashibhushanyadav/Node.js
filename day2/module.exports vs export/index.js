// Note

// EXPORTS
// -while using only exports instead of module.export 
// we get only data in exports parameters which is 
// outside the module and inside the local

// MODULE.EXPORTS
// -while using module.exports instead of exports, 
// It shows the data inside the exports which is 
// inside the module parameters.